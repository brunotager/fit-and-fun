--- src/app/onboarding/page.tsx
+++ src/app/onboarding/page.tsx
@@ -571,10 +571,5 @@
             {/* Custom iOS Keypads */}
-            {[1, 3, 4].includes(step) && (
+            {[3, 4].includes(step) && (
                 <div className="absolute bottom-0 left-0 right-0 z-50">
-                    {step === 1 ? (
-                        <IOSAlphaKeyboard onKeyPress={handleAlphaInput} onDelete={handleAlphaDelete} onReturn={handleNext} />
-                    ) : (
-                        <IOSKeyboard onKeyPress={handleNumInput} onDelete={handleDelete} />
-                    )}
+                    <IOSKeyboard onKeyPress={handleNumInput} onDelete={handleDelete} />
                 </div>
             )}
